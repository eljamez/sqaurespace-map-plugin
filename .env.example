# Google APIs (use a serverless proxy — never expose keys client-side)
# Restrict keys by HTTP referrer to your Squarespace domain(s).
GOOGLE_MAPS_API_KEY=
GOOGLE_SHEETS_API_KEY=

# If using a proxy/serverless function for Sheets
# SHEETS_PROXY_URL=https://your-worker.workers.dev/sheets
